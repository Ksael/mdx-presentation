import {CodeSurfer, CodeSurferColumns, Step} from 'code-surfer'
import { nightOwl, ultramin } from "@code-surfer/themes"

export const theme = ultramin


# ~~Очаровательные~~ презентации с MDX

---

![](https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcR61Pe22ZZnmJwd6fSvKwmFaN49qNwOoO22YA&usqp=CAU)

---

![](https://www.meme-arsenal.com/memes/c6f99c3d54ea39c8fb4d7be9a171de16.jpg)


---

# MDX = Markdown + JSX

---

![](https://thumb.cloud.mail.ru/weblink/thumb/xw1/LA38/mpiaif6KV)

---

## MDX решенения

1. MDX Deck
2. Next MDX Deck
3. Spectacle

---

## MDX решенения

## 1. MDX Deck
2. Next MDX Deck
3. Spectacle

---

## Быстрый старт

<CodeSurfer theme={nightOwl}>

```bash
    mkdir mdx-deck-pres
    cd $_
    yarn init -y
    touch deck.mdx
    yarn add --dev mdx-deck
```

</CodeSurfer>

<Notes>

    - Создает директорию презентации
    - Иницилизирует базовый package.json
    - Инсталит mdx-deck

</Notes>


---
## Особенности MDX Deck

* Режим докладчика
* Все слайды в 1 файле
* Подсветка синтаксиса из коробки

<Notes>

    - Тут я покажу заметки для спикера
    - Заметка 1
    - ПРОДАМ ГАРАЖ

</Notes>

---

# Базовые компонтенты MD

---

## Неупорядоченный список
* Пункт 1
* Пункт 2
* Пункт 3
---

## Упорядоченный список
1. Пункт 1
2. Пункт 2
3. Пункт 3
---
## Таблицы

id | name | price
------|:--------:|------:
1     | шило     | 10
2     | мыло     | 20
3     | веревка  | 40

---

# Code Surfer
---


<CodeSurfer theme={nightOwl}>

```go
    func Printf(format string, a ...interface{}) (n int, err error) {
        return Fprintf(os.Stdout, format, a...)
    }
```

</CodeSurfer>

<Notes>

    - Рассказать что поддерживаются разные языки
    - Для редких языков нужно подгрузить компоненты

</Notes>

---
<CodeSurfer theme={nightOwl}>

```js 1 title="Фокусировка на коде" subtitle="Можно разобрать отдельную строку"
    func Printf(format string, a ...interface{}) (n int, err error) {
        return Fprintf(os.Stdout, format, a...)
    }
```

```js 2 title="Фокусировка на коде" subtitle="а потом вторую"
    func Printf(format string, a ...interface{}) (n int, err error) {
        return Fprintf(os.Stdout, format, a...)
    }
```

</CodeSurfer>

---
<CodeSurfer theme={nightOwl}>

```go
    func ParseInt(s string, base int, bitSize int) (i int64, err error) {
        const fnParseInt = "ParseInt"

        if s == "" {
            return 0, syntaxError(fnParseInt, s)
        }

        // Pick off leading sign.
        s0 := s
        neg := false
        if s[0] == '+' {
            s = s[1:]
        } else if s[0] == '-' {
            neg = true
            s = s[1:]
        }

        ........

        return n, nil
    }

```
```go 1
    func ParseInt(s string, base int, bitSize int) (i int64, err error) {
        const fnParseInt = "ParseInt"

        if s == "" {
            return 0, syntaxError(fnParseInt, s)
        }

        // Pick off leading sign.
        s0 := s
        neg := false
        if s[0] == '+' {
            s = s[1:]
        } else if s[0] == '-' {
            neg = true
            s = s[1:]
        }

        ........

        return n, nil
    }

```

```go 3:6
    func ParseInt(s string, base int, bitSize int) (i int64, err error) {
        const fnParseInt = "ParseInt"

        if s == "" {
            return 0, syntaxError(fnParseInt, s)
        }

        // Pick off leading sign.
        s0 := s
        neg := false
        if s[0] == '+' {
            s = s[1:]
        } else if s[0] == '-' {
            neg = true
            s = s[1:]
        }

        ........

        return n, nil
    }

```

```go 8:17
    func ParseInt(s string, base int, bitSize int) (i int64, err error) {
        const fnParseInt = "ParseInt"

        if s == "" {
            return 0, syntaxError(fnParseInt, s)
        }

        // Pick off leading sign.
        s0 := s
        neg := false
        if s[0] == '+' {
            s = s[1:]
        } else if s[0] == '-' {
            neg = true
            s = s[1:]
        }

        ........

        return n, nil
    }

```
```go 20
    func ParseInt(s string, base int, bitSize int) (i int64, err error) {
        const fnParseInt = "ParseInt"

        if s == "" {
            return 0, syntaxError(fnParseInt, s)
        }

        // Pick off leading sign.
        s0 := s
        neg := false
        if s[0] == '+' {
            s = s[1:]
        } else if s[0] == '-' {
            neg = true
            s = s[1:]
        }

        ........

        return n, nil
    }

```
</CodeSurfer>

---
<CodeSurferColumns themes={[nightOwl, nightOwl]}>

<Step subtitle="Улучшили форматирование" >

```go
    func Printf(format string, a ...interface{}) (n int, err error) {
    return Fprintf(os.Stdout, format, a...)
}
```

```go 1:3
    func Printf(format string, a ...interface{}) (n int, err error) {
        return Fprintf(os.Stdout, format, a...)
    }
```

</Step>

<Step subtitle="Изменили возвращаемый тип" >

```go 1[53:59]
    func Printf(format string, a ...interface{}) (n string, err error) {
        return Fprintf(os.Stdout, format, a...)
    }
```

```go 1[53:56]
    func Printf(format string, a ...interface{}) (n int, err error) {
        return Fprintf(os.Stdout, format, a...)
    }
```

</Step>

</CodeSurferColumns>

---

## Можно экспортировать в

* PDF
* Статический сайт

---

![](http://sun9-49.userapi.com/c852024/v852024792/1655b3/LZLU1ZzElw8.jpg)
